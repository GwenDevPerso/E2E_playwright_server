services:
  playwright:
    build: .
    working_dir: /app
    volumes:
      - ./:/app
    environment:
      - BASE_URL=${BASE_URL}

  report:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./playwright-report:/usr/share/nginx/html